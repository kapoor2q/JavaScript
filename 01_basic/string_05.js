const name="Kapoor"
const repoCount=50;

console.log(name+repoCount+"Value")

console.log(`Hello my Name is ${name} and my repo count is ${repoCount}`)

const strName=new String('Kapoorhc')
// console.log(strName)
// console.log(strName[0])
// console.log(strName.length)
// console.log(strName.toUpperCase())
// console.log(strName.charAt(2))
// console.log(strName.indexOf('t'))

// console.log(strName.includes('Hi'))
let strname2='kapoor Kumar'
//console.log(strName.anchor('kumar'));
let index = 5;

//console.log(`An index of ${index} returns the character ${strname2.at(index)}`);
// Expected output: "An index of 5 returns the character u"

index = -4;

//console.log(`An index of ${index} returns the character ${strname2.at(index)}`);
// Expected output: "An index of -4 returns the character d"

const sentence = 'The quick brown fox jumps over the lazy dog.';

const index1 = 4;

console.log(
  `Character code ${sentence.charCodeAt(index1)} is equal to ${sentence.charAt(
    index1,
  )}`,
);
// Expected output: "Character code 113 is equal to q"
